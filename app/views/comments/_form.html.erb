<%= form_with(model: [course, comment]) do |f| %>
  <div class="form-control">
    <%= f.rich_text_area :body, placeholder: 'Write your comment here...' %>

    <%= f.submit 'Submit', class: 'btn btn-secondary mt-1' %>
  </div>
<% end %>
